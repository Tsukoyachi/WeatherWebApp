<!DOCTYPE html>
<header>
    <meta http-equiv="Content-Type" content="text/html;charset=UTF-8">
    <link rel="stylesheet" href="/css/style.css">
</header>
<body>
    <h1>Weather Web App</h1>
    <p>Search a city and get your weather</p>
    <form action="/" method="post">
        <input type="text" name="location" id="location">
        <input type="submit" value="submit">
    </form>
    <% if(error !== null) {%>
        <h2><%= error %></h2>
    <% }%>
    <% if(location !== null && condition !== null && iconUrl !== null && tempC !== null && tempF !== null) { %>
        <div class="result">
            <img src="<%=iconUrl%>" alt="Weather condition icon">
            <h2>The weather in <%= location%> is <%= condition %> with a temperature of <%= tempC%> °C / <%= tempF%> °F.</h2>
        </div>
    <% } %>
</body>
</html>